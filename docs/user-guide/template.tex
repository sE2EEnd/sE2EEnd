\documentclass[$if(fontsize)$$fontsize$,$endif$a4paper,landscape]{article}

% ── Packages ────────────────────────────────────────────────────────────────
\usepackage[landscape,a4paper,top=2.4cm,bottom=1.8cm,left=2.4cm,right=2.4cm]{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{caption}
\usepackage{array}
\usepackage{calc}
\usepackage{hyperref}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\renewcommand{\familydefault}{\sfdefault}

% ── Brand colors ────────────────────────────────────────────────────────────
\definecolor{primary}{HTML}{2563EB}
\definecolor{primarydark}{HTML}{1D4ED8}
\definecolor{primarylight}{HTML}{EFF6FF}
\definecolor{textgray}{HTML}{6B7280}
\definecolor{bordergray}{HTML}{E5E7EB}
\definecolor{accent}{HTML}{F59E0B}

% ── Hyperlinks ───────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=primary,
  urlcolor=primary,
  hidelinks
}

% ── Section titles ───────────────────────────────────────────────────────────
\titleformat{\section}
  {\Large\bfseries\color{primary}}
  {}{0em}{}
  [\vspace{2pt}\color{bordergray}\titlerule\vspace{4pt}]

\titleformat{\subsection}
  {\normalsize\bfseries\color{primarydark}}
  {}{0em}{}

\titlespacing{\section}{0pt}{18pt}{8pt}
\titlespacing{\subsection}{0pt}{12pt}{4pt}

% ── Callout box (blockquote) ─────────────────────────────────────────────────
\newsavebox{\calloutbox}
\renewenvironment{quote}{%
  \vspace{8pt}%
  \begin{lrbox}{\calloutbox}%
  \begin{minipage}{\dimexpr\linewidth-20pt\relax}%
  \setlength{\parskip}{4pt}%
  \color{primarydark}\small%
}{%
  \end{minipage}%
  \end{lrbox}%
  \noindent\setlength{\fboxsep}{10pt}%
  \colorbox{primarylight}{\usebox{\calloutbox}}%
  \vspace{8pt}\par%
}

% ── Table style ──────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.5}
\setlength{\tabcolsep}{10pt}

% ── Misc ─────────────────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\setlength{\headheight}{34pt}
\addtolength{\topmargin}{-22pt}

% ── Pandoc compat ────────────────────────────────────────────────────────────
\providecommand{\tightlist}{\setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}

% ── longtable / hyperref fix ─────────────────────────────────────────────────
\newcounter{none}

% ── Header / Footer (content pages) ─────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{%
  \includegraphics[height=0.7cm]{../../frontend/core/public/se2eend-logo.png}%
}
\fancyhead[R]{\small\color{textgray} $if(header-title)$$header-title$$else$User Guide$endif$}
\fancyfoot[L]{\small\color{textgray} \textbf{sE2EEnd} — $if(footer-tagline)$$footer-tagline$$else$End-to-End Encrypted File Transfer$endif$}
\fancyfoot[R]{\small\color{textgray} \thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\color{bordergray}\hrule width\headwidth height\headrulewidth}
\renewcommand{\footrulewidth}{0pt}

% ════════════════════════════════════════════════════════════════════════════
\begin{document}

% ── Cover page ───────────────────────────────────────────────────────────────
\thispagestyle{empty}
\pagecolor{primary}

\vspace*{2.8cm}

\begin{center}
  \includegraphics[height=2.8cm]{../../frontend/core/public/se2eend-logo.png}

  \vspace{0.9cm}

  {\fontsize{42}{50}\selectfont\bfseries\color{white} sE2EEnd}

  \vspace{0.5cm}

  % Pronunciation box
  {%
    \setlength{\fboxsep}{7pt}%
    \setlength{\fboxrule}{1.2pt}%
    \fcolorbox{white!40!primary}{primary!85!black}{%
      {\color{white!80}\large\textit{/ s\ensuremath{\varepsilon}nd /}}\quad
      $if(french)$
        {\color{white!55}\normalsize (se prononce~\guillemotleft~}\textcolor{accent}{\normalsize\textbf{Send}}{\color{white!55}\normalsize~\guillemotright)}%
      $else$
        {\color{white!55}\normalsize (pronounced~\guillemotleft}\textcolor{accent}{\normalsize\textbf{Send}}{\color{white!55}\normalsize\guillemotright)}%
      $endif$
    }%
  }

  \vspace{0.7cm}

  {\fontsize{18}{24}\selectfont\color{white!75} $if(cover-subtitle)$$cover-subtitle$$else$User Guide$endif$}

  \vspace{1.6cm}

  {\normalsize\color{white!50} $if(cover-version)$$cover-version$$else$Version 1.0$endif$ \quad — \quad $if(cover-date)$$cover-date$$else$April 2026$endif$}
\end{center}

\newpage
\pagecolor{white}

% ── Document body ────────────────────────────────────────────────────────────
$body$

\end{document}
